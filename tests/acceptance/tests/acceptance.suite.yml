actor: AcceptanceTester
modules:
    enabled:
        - PhpBrowser:
            url: http://localhost
        - \Helper\Acceptance
        - Cli
        - Db
    config:
        Db:
            dsn: 'mysql:host=localhost:8082;dbname=wordpress'
            user: 'wordpress'
            password: 'wordpress'
            dump: 'tests/_data/dump.sql'
            populate: true
            cleanup: true
            reconnect: true
